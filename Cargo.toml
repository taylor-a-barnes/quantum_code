[package]
name = "electron"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
serde_json = "1"

[dev-dependencies]
mockito = "1"
tempfile = "3"
