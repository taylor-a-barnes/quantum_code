[package]
name = "electron"
version = "0.1.0"
edition = "2024"

[dependencies]
faer = "0.19"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
serde_json = "1"
serde_yaml = "0.9"

[dev-dependencies]
mockito = "1"
tempfile = "3"
